# Workflow de compilação do APK profissional
name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'

      - name: Extract Android project
        shell: bash
        run: |
          set -e
          rm -rf extracted-project
          mkdir -p extracted-project
          unzip -q CapturaCubagemAndroid-projeto.zip -d extracted-project

      - name: Locate Gradle project
        id: locate
        shell: bash
        run: |
          set -e
          SETTINGS=$(find extracted-project -type f \( -name settings.gradle -o -name settings.gradle.kts \) | head -n 1)
          if [ -z "$SETTINGS" ]; then
            echo "settings.gradle not found inside project ZIP"
            exit 1
          fi
          PROJECT_DIR=$(dirname "$SETTINGS")
          echo "project_dir=$PROJECT_DIR" >> "$GITHUB_OUTPUT"
          echo "Gradle project: $PROJECT_DIR"

      - name: Apply professional interface
        shell: bash
        run: |
          set -e
          PROJECT_DIR="${{ steps.locate.outputs.project_dir }}"
          MAIN=$(find "$PROJECT_DIR/app/src/main/java" -type f -name MainActivity.java | head -n 1)
          if [ -z "$MAIN" ]; then
            echo "MainActivity.java not found"
            exit 1
          fi
          PACKAGE=$(sed -n -E 's/^package[[:space:]]+([^;]+);/\1/p' "$MAIN" | head -n 1)
          if [ -z "$PACKAGE" ]; then
            echo "Package not found in MainActivity.java"
            exit 1
          fi
          sed "s/__PACKAGE__/$PACKAGE/g" professional/MainActivity.java.template > "$MAIN"
          grep -q "CUBAGEM 68 PRO" "$MAIN"
          echo "Professional interface applied to $MAIN"

      - name: Fix Android SDK level
        shell: bash
        run: |
          set -e
          PROJECT_DIR="${{ steps.locate.outputs.project_dir }}"
          find "$PROJECT_DIR" -type f \( -name 'build.gradle' -o -name 'build.gradle.kts' \) -print0 | while IFS= read -r -d '' FILE; do
            sed -i -E 's/(compileSdkVersion[[:space:]]+)[0-9]+/\135/g' "$FILE"
            sed -i -E 's/(compileSdk[[:space:]]*=[[:space:]]*)[0-9]+/\135/g' "$FILE"
            sed -i -E 's/(compileSdk[[:space:]]+)[0-9]+/\135/g' "$FILE"
          done

      - name: Repair AndroidManifest XML
        shell: bash
        run: |
          set -e
          PROJECT_DIR="${{ steps.locate.outputs.project_dir }}"
          MAIN=$(find "$PROJECT_DIR/app/src/main/java" -type f -name MainActivity.java | head -n 1)
          PACKAGE=$(sed -n -E 's/^package[[:space:]]+([^;]+);/\1/p' "$MAIN" | head -n 1)
          MANIFEST="$PROJECT_DIR/app/src/main/AndroidManifest.xml"
          mkdir -p "$(dirname "$MANIFEST")"
          cat > "$MANIFEST" <<EOF
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.CAMERA" />
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
              <uses-feature android:name="android.hardware.camera" android:required="false" />
              <application
                  android:allowBackup="true"
                  android:label="Cubagem 68 Pro"
                  android:supportsRtl="true"
                  android:theme="@style/AppTheme">
                  <activity
                      android:name="$PACKAGE.MainActivity"
                      android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF
          python3 -c "import xml.etree.ElementTree as ET; ET.parse('$MANIFEST'); print('AndroidManifest.xml válido')"

      - name: Build debug APK
        working-directory: ${{ steps.locate.outputs.project_dir }}
        run: gradle assembleDebug --stacktrace

      - name: Find APK
        id: apk
        shell: bash
        run: |
          set -e
          APK=$(find "${{ steps.locate.outputs.project_dir }}" -type f -path '*/build/outputs/apk/debug/*.apk' | head -n 1)
          if [ -z "$APK" ]; then
            echo "APK not found after build"
            exit 1
          fi
          echo "apk_path=$APK" >> "$GITHUB_OUTPUT"
          echo "APK: $APK"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Cubagem68Pro-NOVA-VERSAO
          path: ${{ steps.apk.outputs.apk_path }}
          if-no-files-found: error
